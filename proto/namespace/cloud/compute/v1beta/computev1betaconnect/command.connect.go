// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: proto/namespace/cloud/compute/v1beta/command.proto

package computev1betaconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1beta "namespacelabs.dev/integrations/proto/namespace/cloud/compute/v1beta"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// CommandServiceName is the fully-qualified name of the CommandService service.
	CommandServiceName = "namespace.cloud.compute.v1beta.CommandService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// CommandServiceRunCommandProcedure is the fully-qualified name of the CommandService's RunCommand
	// RPC.
	CommandServiceRunCommandProcedure = "/namespace.cloud.compute.v1beta.CommandService/RunCommand"
	// CommandServiceRunCommandSyncProcedure is the fully-qualified name of the CommandService's
	// RunCommandSync RPC.
	CommandServiceRunCommandSyncProcedure = "/namespace.cloud.compute.v1beta.CommandService/RunCommandSync"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	commandServiceServiceDescriptor              = v1beta.File_proto_namespace_cloud_compute_v1beta_command_proto.Services().ByName("CommandService")
	commandServiceRunCommandMethodDescriptor     = commandServiceServiceDescriptor.Methods().ByName("RunCommand")
	commandServiceRunCommandSyncMethodDescriptor = commandServiceServiceDescriptor.Methods().ByName("RunCommandSync")
)

// CommandServiceClient is a client for the namespace.cloud.compute.v1beta.CommandService service.
type CommandServiceClient interface {
	// Runs a command in a container within a running instance. Stdout and stderr
	// are streamed back as individual responses. The final response includes an
	// ExitStatus with the command's exit code.
	//
	// ### Errors
	// - If the instance does not exist, a `NotFound` status is returned.
	// - If the target container does not exist, a `NotFound` status is returned.
	RunCommand(context.Context, *connect.Request[v1beta.RunCommandRequest]) (*connect.ServerStreamForClient[v1beta.RunCommandResponse], error)
	// Runs a command in a container within a running instance and waits for it
	// to complete. Stdout and stderr are bundled into a single response,
	// similar to CombinedOutput.
	//
	// ### Errors
	// - If the instance does not exist, a `NotFound` status is returned.
	// - If the target container does not exist, a `NotFound` status is returned.
	RunCommandSync(context.Context, *connect.Request[v1beta.RunCommandRequest]) (*connect.Response[v1beta.RunCommandSyncResponse], error)
}

// NewCommandServiceClient constructs a client for the namespace.cloud.compute.v1beta.CommandService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewCommandServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) CommandServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &commandServiceClient{
		runCommand: connect.NewClient[v1beta.RunCommandRequest, v1beta.RunCommandResponse](
			httpClient,
			baseURL+CommandServiceRunCommandProcedure,
			connect.WithSchema(commandServiceRunCommandMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
		runCommandSync: connect.NewClient[v1beta.RunCommandRequest, v1beta.RunCommandSyncResponse](
			httpClient,
			baseURL+CommandServiceRunCommandSyncProcedure,
			connect.WithSchema(commandServiceRunCommandSyncMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// commandServiceClient implements CommandServiceClient.
type commandServiceClient struct {
	runCommand     *connect.Client[v1beta.RunCommandRequest, v1beta.RunCommandResponse]
	runCommandSync *connect.Client[v1beta.RunCommandRequest, v1beta.RunCommandSyncResponse]
}

// RunCommand calls namespace.cloud.compute.v1beta.CommandService.RunCommand.
func (c *commandServiceClient) RunCommand(ctx context.Context, req *connect.Request[v1beta.RunCommandRequest]) (*connect.ServerStreamForClient[v1beta.RunCommandResponse], error) {
	return c.runCommand.CallServerStream(ctx, req)
}

// RunCommandSync calls namespace.cloud.compute.v1beta.CommandService.RunCommandSync.
func (c *commandServiceClient) RunCommandSync(ctx context.Context, req *connect.Request[v1beta.RunCommandRequest]) (*connect.Response[v1beta.RunCommandSyncResponse], error) {
	return c.runCommandSync.CallUnary(ctx, req)
}

// CommandServiceHandler is an implementation of the namespace.cloud.compute.v1beta.CommandService
// service.
type CommandServiceHandler interface {
	// Runs a command in a container within a running instance. Stdout and stderr
	// are streamed back as individual responses. The final response includes an
	// ExitStatus with the command's exit code.
	//
	// ### Errors
	// - If the instance does not exist, a `NotFound` status is returned.
	// - If the target container does not exist, a `NotFound` status is returned.
	RunCommand(context.Context, *connect.Request[v1beta.RunCommandRequest], *connect.ServerStream[v1beta.RunCommandResponse]) error
	// Runs a command in a container within a running instance and waits for it
	// to complete. Stdout and stderr are bundled into a single response,
	// similar to CombinedOutput.
	//
	// ### Errors
	// - If the instance does not exist, a `NotFound` status is returned.
	// - If the target container does not exist, a `NotFound` status is returned.
	RunCommandSync(context.Context, *connect.Request[v1beta.RunCommandRequest]) (*connect.Response[v1beta.RunCommandSyncResponse], error)
}

// NewCommandServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewCommandServiceHandler(svc CommandServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	commandServiceRunCommandHandler := connect.NewServerStreamHandler(
		CommandServiceRunCommandProcedure,
		svc.RunCommand,
		connect.WithSchema(commandServiceRunCommandMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	commandServiceRunCommandSyncHandler := connect.NewUnaryHandler(
		CommandServiceRunCommandSyncProcedure,
		svc.RunCommandSync,
		connect.WithSchema(commandServiceRunCommandSyncMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/namespace.cloud.compute.v1beta.CommandService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case CommandServiceRunCommandProcedure:
			commandServiceRunCommandHandler.ServeHTTP(w, r)
		case CommandServiceRunCommandSyncProcedure:
			commandServiceRunCommandSyncHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedCommandServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedCommandServiceHandler struct{}

func (UnimplementedCommandServiceHandler) RunCommand(context.Context, *connect.Request[v1beta.RunCommandRequest], *connect.ServerStream[v1beta.RunCommandResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("namespace.cloud.compute.v1beta.CommandService.RunCommand is not implemented"))
}

func (UnimplementedCommandServiceHandler) RunCommandSync(context.Context, *connect.Request[v1beta.RunCommandRequest]) (*connect.Response[v1beta.RunCommandSyncResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("namespace.cloud.compute.v1beta.CommandService.RunCommandSync is not implemented"))
}

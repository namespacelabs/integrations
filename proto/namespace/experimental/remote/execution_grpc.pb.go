// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.1
// - protoc             (unknown)
// source: proto/namespace/experimental/remote/execution.proto

package remote

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	ExecutionService_Execute_FullMethodName       = "/namespace.experimental.remote.ExecutionService/Execute"
	ExecutionService_FetchSnapshot_FullMethodName = "/namespace.experimental.remote.ExecutionService/FetchSnapshot"
)

// ExecutionServiceClient is the client API for ExecutionService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// ExecutionService coordinates local command executions and snapshot
// fetching, providing deduplication when multiple callers request the
// same operation concurrently.
//
// The service is intended to be run as a local daemon by the "remote"
// CLI. When multiple CLI invocations target the same inputs, only one
// execution is performed and the result is shared with all callers.
//
// ## Execution deduplication
//
//  1. Call `Execute` with the input specification and command.
//  2. The service checks the remote cache, joins an in-progress
//     execution with the same inputs, or starts a new one.
//  3. The response streams status updates and the final result.
//
// ## Snapshot deduplication
//
//  1. Call `FetchSnapshot` with a result reference and target directory.
//  2. If another caller is already fetching the same snapshot, the
//     download is shared and both callers receive the result.
type ExecutionServiceClient interface {
	// Execute runs a command with the given inputs.
	//
	// If a cached result exists for the inputs, it is returned immediately.
	// If an identical execution is already in progress (same input set),
	// the caller joins it and receives the result when it completes.
	// Otherwise, a new execution is started.
	//
	// The response is a stream of events: status updates, optional log
	// output, and a terminal result event.
	Execute(ctx context.Context, in *ExecuteRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ExecuteEvent], error)
	// FetchSnapshot downloads a snapshot to a local directory.
	//
	// If another caller is already fetching the same snapshot, the
	// request joins the in-progress download. The response is returned
	// once the snapshot is fully available locally.
	FetchSnapshot(ctx context.Context, in *FetchSnapshotRequest, opts ...grpc.CallOption) (*FetchSnapshotResponse, error)
}

type executionServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewExecutionServiceClient(cc grpc.ClientConnInterface) ExecutionServiceClient {
	return &executionServiceClient{cc}
}

func (c *executionServiceClient) Execute(ctx context.Context, in *ExecuteRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ExecuteEvent], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &ExecutionService_ServiceDesc.Streams[0], ExecutionService_Execute_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[ExecuteRequest, ExecuteEvent]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type ExecutionService_ExecuteClient = grpc.ServerStreamingClient[ExecuteEvent]

func (c *executionServiceClient) FetchSnapshot(ctx context.Context, in *FetchSnapshotRequest, opts ...grpc.CallOption) (*FetchSnapshotResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(FetchSnapshotResponse)
	err := c.cc.Invoke(ctx, ExecutionService_FetchSnapshot_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ExecutionServiceServer is the server API for ExecutionService service.
// All implementations must embed UnimplementedExecutionServiceServer
// for forward compatibility.
//
// ExecutionService coordinates local command executions and snapshot
// fetching, providing deduplication when multiple callers request the
// same operation concurrently.
//
// The service is intended to be run as a local daemon by the "remote"
// CLI. When multiple CLI invocations target the same inputs, only one
// execution is performed and the result is shared with all callers.
//
// ## Execution deduplication
//
//  1. Call `Execute` with the input specification and command.
//  2. The service checks the remote cache, joins an in-progress
//     execution with the same inputs, or starts a new one.
//  3. The response streams status updates and the final result.
//
// ## Snapshot deduplication
//
//  1. Call `FetchSnapshot` with a result reference and target directory.
//  2. If another caller is already fetching the same snapshot, the
//     download is shared and both callers receive the result.
type ExecutionServiceServer interface {
	// Execute runs a command with the given inputs.
	//
	// If a cached result exists for the inputs, it is returned immediately.
	// If an identical execution is already in progress (same input set),
	// the caller joins it and receives the result when it completes.
	// Otherwise, a new execution is started.
	//
	// The response is a stream of events: status updates, optional log
	// output, and a terminal result event.
	Execute(*ExecuteRequest, grpc.ServerStreamingServer[ExecuteEvent]) error
	// FetchSnapshot downloads a snapshot to a local directory.
	//
	// If another caller is already fetching the same snapshot, the
	// request joins the in-progress download. The response is returned
	// once the snapshot is fully available locally.
	FetchSnapshot(context.Context, *FetchSnapshotRequest) (*FetchSnapshotResponse, error)
	mustEmbedUnimplementedExecutionServiceServer()
}

// UnimplementedExecutionServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedExecutionServiceServer struct{}

func (UnimplementedExecutionServiceServer) Execute(*ExecuteRequest, grpc.ServerStreamingServer[ExecuteEvent]) error {
	return status.Error(codes.Unimplemented, "method Execute not implemented")
}
func (UnimplementedExecutionServiceServer) FetchSnapshot(context.Context, *FetchSnapshotRequest) (*FetchSnapshotResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method FetchSnapshot not implemented")
}
func (UnimplementedExecutionServiceServer) mustEmbedUnimplementedExecutionServiceServer() {}
func (UnimplementedExecutionServiceServer) testEmbeddedByValue()                          {}

// UnsafeExecutionServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to ExecutionServiceServer will
// result in compilation errors.
type UnsafeExecutionServiceServer interface {
	mustEmbedUnimplementedExecutionServiceServer()
}

func RegisterExecutionServiceServer(s grpc.ServiceRegistrar, srv ExecutionServiceServer) {
	// If the following call panics, it indicates UnimplementedExecutionServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&ExecutionService_ServiceDesc, srv)
}

func _ExecutionService_Execute_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(ExecuteRequest)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(ExecutionServiceServer).Execute(m, &grpc.GenericServerStream[ExecuteRequest, ExecuteEvent]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type ExecutionService_ExecuteServer = grpc.ServerStreamingServer[ExecuteEvent]

func _ExecutionService_FetchSnapshot_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(FetchSnapshotRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ExecutionServiceServer).FetchSnapshot(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ExecutionService_FetchSnapshot_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ExecutionServiceServer).FetchSnapshot(ctx, req.(*FetchSnapshotRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// ExecutionService_ServiceDesc is the grpc.ServiceDesc for ExecutionService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var ExecutionService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "namespace.experimental.remote.ExecutionService",
	HandlerType: (*ExecutionServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "FetchSnapshot",
			Handler:    _ExecutionService_FetchSnapshot_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Execute",
			Handler:       _ExecutionService_Execute_Handler,
			ServerStreams: true,
		},
	},
	Metadata: "proto/namespace/experimental/remote/execution.proto",
}

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: proto/namespace/experimental/remote/execution.proto

package remoteconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	remote "namespacelabs.dev/integrations/proto/namespace/experimental/remote"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ExecutionServiceName is the fully-qualified name of the ExecutionService service.
	ExecutionServiceName = "namespace.experimental.remote.ExecutionService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ExecutionServiceExecuteProcedure is the fully-qualified name of the ExecutionService's Execute
	// RPC.
	ExecutionServiceExecuteProcedure = "/namespace.experimental.remote.ExecutionService/Execute"
	// ExecutionServiceFetchSnapshotProcedure is the fully-qualified name of the ExecutionService's
	// FetchSnapshot RPC.
	ExecutionServiceFetchSnapshotProcedure = "/namespace.experimental.remote.ExecutionService/FetchSnapshot"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	executionServiceServiceDescriptor             = remote.File_proto_namespace_experimental_remote_execution_proto.Services().ByName("ExecutionService")
	executionServiceExecuteMethodDescriptor       = executionServiceServiceDescriptor.Methods().ByName("Execute")
	executionServiceFetchSnapshotMethodDescriptor = executionServiceServiceDescriptor.Methods().ByName("FetchSnapshot")
)

// ExecutionServiceClient is a client for the namespace.experimental.remote.ExecutionService
// service.
type ExecutionServiceClient interface {
	// Execute runs a command with the given inputs.
	//
	// If a cached result exists for the inputs, it is returned immediately.
	// If an identical execution is already in progress (same input set),
	// the caller joins it and receives the result when it completes.
	// Otherwise, a new execution is started.
	//
	// The response is a stream of events: status updates, optional log
	// output, and a terminal result event.
	Execute(context.Context, *connect.Request[remote.ExecuteRequest]) (*connect.ServerStreamForClient[remote.ExecuteEvent], error)
	// FetchSnapshot downloads a snapshot to a local directory.
	//
	// If another caller is already fetching the same snapshot, the
	// request joins the in-progress download. The response is returned
	// once the snapshot is fully available locally.
	FetchSnapshot(context.Context, *connect.Request[remote.FetchSnapshotRequest]) (*connect.Response[remote.FetchSnapshotResponse], error)
}

// NewExecutionServiceClient constructs a client for the
// namespace.experimental.remote.ExecutionService service. By default, it uses the Connect protocol
// with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To
// use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb()
// options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewExecutionServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ExecutionServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &executionServiceClient{
		execute: connect.NewClient[remote.ExecuteRequest, remote.ExecuteEvent](
			httpClient,
			baseURL+ExecutionServiceExecuteProcedure,
			connect.WithSchema(executionServiceExecuteMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
		fetchSnapshot: connect.NewClient[remote.FetchSnapshotRequest, remote.FetchSnapshotResponse](
			httpClient,
			baseURL+ExecutionServiceFetchSnapshotProcedure,
			connect.WithSchema(executionServiceFetchSnapshotMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// executionServiceClient implements ExecutionServiceClient.
type executionServiceClient struct {
	execute       *connect.Client[remote.ExecuteRequest, remote.ExecuteEvent]
	fetchSnapshot *connect.Client[remote.FetchSnapshotRequest, remote.FetchSnapshotResponse]
}

// Execute calls namespace.experimental.remote.ExecutionService.Execute.
func (c *executionServiceClient) Execute(ctx context.Context, req *connect.Request[remote.ExecuteRequest]) (*connect.ServerStreamForClient[remote.ExecuteEvent], error) {
	return c.execute.CallServerStream(ctx, req)
}

// FetchSnapshot calls namespace.experimental.remote.ExecutionService.FetchSnapshot.
func (c *executionServiceClient) FetchSnapshot(ctx context.Context, req *connect.Request[remote.FetchSnapshotRequest]) (*connect.Response[remote.FetchSnapshotResponse], error) {
	return c.fetchSnapshot.CallUnary(ctx, req)
}

// ExecutionServiceHandler is an implementation of the
// namespace.experimental.remote.ExecutionService service.
type ExecutionServiceHandler interface {
	// Execute runs a command with the given inputs.
	//
	// If a cached result exists for the inputs, it is returned immediately.
	// If an identical execution is already in progress (same input set),
	// the caller joins it and receives the result when it completes.
	// Otherwise, a new execution is started.
	//
	// The response is a stream of events: status updates, optional log
	// output, and a terminal result event.
	Execute(context.Context, *connect.Request[remote.ExecuteRequest], *connect.ServerStream[remote.ExecuteEvent]) error
	// FetchSnapshot downloads a snapshot to a local directory.
	//
	// If another caller is already fetching the same snapshot, the
	// request joins the in-progress download. The response is returned
	// once the snapshot is fully available locally.
	FetchSnapshot(context.Context, *connect.Request[remote.FetchSnapshotRequest]) (*connect.Response[remote.FetchSnapshotResponse], error)
}

// NewExecutionServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewExecutionServiceHandler(svc ExecutionServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	executionServiceExecuteHandler := connect.NewServerStreamHandler(
		ExecutionServiceExecuteProcedure,
		svc.Execute,
		connect.WithSchema(executionServiceExecuteMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	executionServiceFetchSnapshotHandler := connect.NewUnaryHandler(
		ExecutionServiceFetchSnapshotProcedure,
		svc.FetchSnapshot,
		connect.WithSchema(executionServiceFetchSnapshotMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/namespace.experimental.remote.ExecutionService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ExecutionServiceExecuteProcedure:
			executionServiceExecuteHandler.ServeHTTP(w, r)
		case ExecutionServiceFetchSnapshotProcedure:
			executionServiceFetchSnapshotHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedExecutionServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedExecutionServiceHandler struct{}

func (UnimplementedExecutionServiceHandler) Execute(context.Context, *connect.Request[remote.ExecuteRequest], *connect.ServerStream[remote.ExecuteEvent]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("namespace.experimental.remote.ExecutionService.Execute is not implemented"))
}

func (UnimplementedExecutionServiceHandler) FetchSnapshot(context.Context, *connect.Request[remote.FetchSnapshotRequest]) (*connect.Response[remote.FetchSnapshotResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("namespace.experimental.remote.ExecutionService.FetchSnapshot is not implemented"))
}
